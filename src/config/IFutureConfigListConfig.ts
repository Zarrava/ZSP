/**
 * Placeholder for loading tenant overrides from a SharePoint configuration list.
 * Not active in the current release — defaults remain in code.
 */
export interface IFutureConfigListConfig {
  enabled: boolean;
  listTitle?: string;
}
