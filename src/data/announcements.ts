import { IAnnouncement } from '../models/Announcement';

/**
 * Localhost mock list. Production reads the ZEF Announcements SharePoint list.
 * Dummy announcement records have been removed.
 */
export const mockAnnouncements: IAnnouncement[] = [];

export const announcementsViewAllUrl: string | undefined = undefined;
