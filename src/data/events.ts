import { IEvent } from '../models/Event';

/**
 * Unused localhost sample. Production events come from the signed-in member's calendar.
 * Dummy event records have been removed.
 */
export const mockEvents: IEvent[] = [];

export const eventsViewAllUrl: string | undefined = undefined;
