import { IResource } from '../models/Resource';

/**
 * Localhost mock list. Production reads the ZEF Resources SharePoint list.
 * Generic Microsoft help samples have been removed.
 */
export const mockResources: IResource[] = [];
